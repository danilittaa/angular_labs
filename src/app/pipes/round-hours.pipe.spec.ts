import { RoundHoursPipe } from './round-hours.pipe';

describe('RoundHoursPipe', () => {
  it('create an instance', () => {
    const pipe = new RoundHoursPipe();
    expect(pipe).toBeTruthy();
  });
});
